{
  "buildCommand": "npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://business-analysis.railway.app/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
